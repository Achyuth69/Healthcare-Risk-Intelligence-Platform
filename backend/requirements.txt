# ============================================================
# Healthcare Risk Intelligence Platform — Full Requirements
# Python 3.11+ (tested on 3.13)
# ============================================================

# ── Core Framework ────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.6
pydantic-settings==2.7.0
python-multipart==0.0.9

# ── Database ──────────────────────────────────────────────────
sqlalchemy==2.0.36
asyncpg==0.30.0          # PostgreSQL async driver (production)
aiosqlite==0.20.0        # SQLite async driver (local dev)
alembic==1.14.0
psycopg2-binary==2.9.9   # Sync fallback for migrations

# ── Auth & Security ───────────────────────────────────────────
python-jose[cryptography]==3.5.0
bcrypt==4.2.1
cryptography==44.0.2
python-dotenv==1.2.2
email-validator==2.2.0

# ── ML / Data Science ─────────────────────────────────────────
numpy==2.2.6
pandas==2.2.3
scikit-learn==1.6.1
xgboost==3.0.2
lightgbm==4.6.0
joblib==1.5.1
optuna==3.6.1
imbalanced-learn==0.12.3

# ── Explainability ────────────────────────────────────────────
shap==0.47.2
lime==0.2.0.1

# ── LLM / Fine-Tuning (optional — needs GPU) ──────────────────
# Uncomment on GPU instances:
# torch==2.3.0
# transformers==4.41.1
# peft==0.11.1
# trl==0.8.6
# bitsandbytes==0.43.1
# accelerate==0.30.1
# datasets==2.19.1
# sentencepiece==0.2.0

# ── RAG Pipeline (optional — needs ChromaDB running) ──────────
# Uncomment when ChromaDB is available:
# langchain==0.2.1
# langchain-community==0.2.1
# chromadb==0.5.0
# sentence-transformers==3.0.0
# pypdf==4.2.0
# tiktoken==0.7.0

# ── Caching ───────────────────────────────────────────────────
redis==5.2.1

# ── Monitoring ────────────────────────────────────────────────
prometheus-fastapi-instrumentator==7.0.0
structlog==25.3.0

# ── HTTP Client ───────────────────────────────────────────────
httpx==0.28.1
aiofiles==25.1.0

# ── AWS (optional) ────────────────────────────────────────────
# boto3==1.34.110

# ── Testing ───────────────────────────────────────────────────
pytest==8.3.5
pytest-asyncio==0.24.0
pytest-cov==6.1.0
